# Docker Environment Configuration for PayloadCMS
# Copy this file to .env and update the values as needed

# Database connection string - Use 'mongo' hostname for Docker Compose
DATABASE_URI=mongodb://mongo:27017/recipes
# Or use a PostgreSQL connection string
#DATABASE_URI=postgresql://postgres:5432/your-database-name

# Used to encrypt JWT tokens - CHANGE THIS IN PRODUCTION! 
# openssl rand -base64 32
PAYLOAD_SECRET=YOUR_PAYLOAD_SECRET_HERE

# Used to configure CORS, format links and more. No trailing slash
# For local development
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
# For production (Coolify)
#NEXT_PUBLIC_SERVER_URL=https://payloadcms.traditionalflavor.com.tr

# Secret used to authenticate cron jobs - CHANGE THIS IN PRODUCTION!
# openssl rand -base64 32
CRON_SECRET=YOUR_CRON_SECRET_HERE

# Used to validate preview requests - CHANGE THIS IN PRODUCTION!
# openssl rand -base64 32
PREVIEW_SECRET=YOUR_PREVIEW_SECRET_HERE

# Optional: Email Configuration
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# FROM_EMAIL=noreply@traditionalflavor.com.tr
# FROM_NAME=Traditional Flavor

# Optional: File Storage Configuration (for cloud storage)
# S3_ENDPOINT=
# S3_BUCKET=traditionalflavor-recipes
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_REGION=eu-central-1

# Optional: Analytics and Monitoring
# NEXT_TELEMETRY_DISABLED=1

# Node Environment
NODE_ENV=development
